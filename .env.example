# AI File Organizer Configuration

# Background Monitor Configuration
# Comma-separated list of paths to watch for new files
# Use ~ for home directory
AUTO_MONITOR_PATHS=~/Downloads,~/Desktop

# Scan interval in seconds (optional, default: 5)
AUTO_MONITOR_INTERVAL=5

# Optional: Google Drive integration path
# GOOGLE_DRIVE_PATH=~/GoogleDrive

# Optional: Database path (defaults to ./data/organizer.db)
# DATABASE_PATH=./data/organizer.db

# Optional: Confidence mode (NEVER, MINIMAL, SMART, ALWAYS)
# CONFIDENCE_MODE=SMART
