## 2024-05-22 - Icon-Only Buttons Accessibility
**Learning:** Icon-only buttons (like notifications) often lack `aria-label` or `title`, making them inaccessible to screen readers and confusing for mouse users who don't understand the icon.
**Action:** Always audit icon-only buttons for `aria-label` and `title` attributes, and ensure they have visible focus states for keyboard users.
